# Head service configuration
# Single node
HEAD_NODES=http://node:8001|default
# Multiple nodes (comma separated):
# HEAD_NODES=http://node:8001|default,http://node2:8001|eu-west,http://10.0.0.5:8001|lab

HEAD_PORT=7790

# Optional: where to store generated keys (defaults are fine)
# HEAD_KEY_DIR=./data/head
# HEAD_PRIVATE_KEY_PATH=./data/head/head_private.pem
# HEAD_PUBLIC_KEY_PATH=./data/head/head_public.pem
